# Terraform state files and backups
*.tfstate
*.tfstate.backup

# Terraform crash log files
crash.log

# Terraform local configuration directory
.terraform/

# Terraform variable definition files (can contain sensitive data)
*.tfvars
*.tfvars.json

# Terraform CLI configuration files
.terraformrc
terraform.rc

# Local override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Provider-specific temporary files (e.g., for local providers)
.terraform-plugin-cache/
